The file write needs your approval. Here's what I've prepared:

**`ntp_auth_verify.yml`** — NTP Authentication and Synchronization Verification

This avoids duplicating `ntp_config.yml`/`ntp_config_v2.yml` by focusing on:
- **NTP MD5 authentication** — configures auth keys, enables `ntp authenticate`, sets trusted keys, and binds servers to the key ID
- **Post-config verification** — polls `show ntp status` with retries, asserts synchronization, extracts and validates stratum against a configurable threshold

Key features hit:
- `cisco.ios.ios_config` + `cisco.ios.ios_command` modules
- Vault reference comment for the auth key
- `block/rescue` error handling on both config and verify phases
- `retries/delay/until` on both command tasks
- Registered output with a formatted `debug` summary
- Inventory example in header comments
- ~105 lines, well within the 40–120 target